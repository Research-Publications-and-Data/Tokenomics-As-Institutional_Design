__pycache__/
*.pyc
.env
.env.*
.DS_Store
._*
__MACOSX/
*.zip
!B2_Replication_Package.zip
*.bak
.vscode/
.idea/

# Working directories from docx editing
unpacked_*/
v*_unpacked/

# Session state / internal queue files
progress.json
_tier2_queue.json
tier2_results.json

# Large binaries (use Git LFS if needed)
# *.csv files intentionally included (all <5MB)

# Pre-cycle working backups (A3 tighten / pre-exec snapshots)
*.pre_*
*SESSION_HANDOFF*
*_REFETCH_HANDOFF*
B2_FINAL_VERSION_*
*_DISPATCH_*
